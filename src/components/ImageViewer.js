import { useParams } from "react-router"

export default function ImageViewer() {
    const params = useParams()

    console.log(params)

    return(
        <div>
            
        </div>
    )
}